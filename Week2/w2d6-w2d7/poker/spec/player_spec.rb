require 'rspec'
require 'player.rb'

describe "Player" do
  describe "initialize" do
    it "can be initialized with a name" do
    
    end
    it "gets a name from the user if none is passed"  
    
    it "can be initialzed with a wallet" 
    
  end
  
  describe ".look_at_hand" do 
    it "displays a the players hand"
    it "doesn't freak out if the hand is nil" 
  end
  
  desribe "bet" do
    it "does not let the player bet more than what is in his wallet"
    it "checks that the bet either matches or raises"
    it "adds the bet to the pot"
  end

end